name: Merge branch now
on:
  push:
    branches:
      - 'staging'

jobs:
  merge-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Merge to master
        uses: devmasx/merge-branch@debug
        with:
          type: now
          target_branch: master
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
